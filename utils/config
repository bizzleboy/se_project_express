// utils/config.js

// This is your secret key for JWT. In a real application, you should use a more complex key
// and store it in environment variables for security.
const JWT_SECRET = process.env.JWT_SECRET || 'your_secret_key_here';

module.exports = {
  JWT_SECRET,
  // You can add more configuration constants here
};
